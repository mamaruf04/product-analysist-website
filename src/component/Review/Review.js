import React from 'react';

const Review = (props) => {
    console.log(props.review);
    return (
        <div>
            {/* <h4>{props.review.id}</h4> */}
        </div>
    );
};

export default Review;